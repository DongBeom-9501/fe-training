# 3차 리뷰 — 수정 필요 사항

## 1. 번호 재배치·참조 무결성

- `docs/00-시작하기/01-이-문서를-읽는-법.md:18` — 학습 완료 기준이 여전히 “Part 6까지”입니다. 뒤의 목록에는 데이터 조회·폼 수정·에러 처리를 포함한 신규 화면 개발이 있는데, 현재 이 범위는 Part 8까지 이어집니다. 81~82행의 “실전 애플리케이션” 도달 시점과 일치하도록 Part 8까지로 바꾸세요.

- `docs/00-시작하기/02-BE와-FE의-근본적-차이.md:113` — 서버 데이터를 로컬 state로 복사하는 문제의 참조가 `26–34장`으로 남았습니다. 범위의 끝만 재매핑된 형태입니다. 현재 상태 분류와 서버 상태 관리를 다루는 `33–34장`으로 바꾸세요.

- `docs/부록/A-FE-BE-용어-대응표.md:55` — URL 상태가 `36장`을 가리키지만, 현재 36장은 폼과 검증입니다. URL 상태를 분류하는 `33장`으로 바꾸세요. 라우터 API 설명까지 의도했다면 30장을 별도로 연결하세요.

- `docs/04-React/19-Hooks.md:256` — 컴포넌트 코드가 서버에서도 실행될 수 있다는 설명의 참조가 `Part 5`로 남았습니다. 현재 서버·클라이언트 렌더링을 다루는 `Part 7`, 특히 31장으로 바꾸세요.

- `docs/04-React/20-컴포넌트-설계.md:226` — 다음 장 예고가 “Part 5에서는 서버가 다시 등장합니다”로 남았습니다. 바로 다음은 패키지 매니저와 빌드입니다. 도구와 파이프라인을 예고하도록 문장 전체를 바꾸고, 서버의 재등장은 Part 7로 안내하세요.

- `docs/01-브라우저라는-런타임/03-URL을-입력하면-벌어지는-일.md:53` — HTTP/2 멀티플렉싱을 22·26장에서 설명한다고 예고하지만, 두 장 모두 해당 설명이 없습니다. 26장에 “한 연결에서 여러 요청을 겹쳐 처리한다”는 설명을 추가하거나, 예고하는 기술 목록에서 빼세요. 단순 번호 변경으로는 참조가 완성되지 않습니다.

## 2. 개념 부채·코드 독해의 단절

- `docs/05-도구와-파이프라인/22-번들러와-빌드.md:90` — 처음 등장하는 `lazy`를 정의하지 않고 코드 스플리팅 예시로 사용합니다. `import()`가 모듈을 비동기로 가져오는 문법이고, `lazy`는 그 로더를 React 컴포넌트로 연결하는 API라는 구분이 필요합니다. 예시를 유지한다면 `lazy`의 import, 컴포넌트의 default export, 로딩 중 대체 화면을 담당하는 `Suspense`를 최소한으로 설명하세요. 이 장에서는 일반 `import()` 예시만 쓰는 방법도 있습니다. [React lazy](https://react.dev/reference/react/lazy)

- `docs/06-성능과-보안/24-Core-Web-Vitals.md:90` — `스켈레톤`을 앞 장이나 이 장에서 정의하지 않은 채 CLS 해법으로 사용합니다. “실제 콘텐츠가 오기 전에 그 위치와 모양을 대신 표시하는 로딩 UI”라고 먼저 설명하고, 예상 높이를 확보하는 예를 붙이세요.

- `docs/06-성능과-보안/27-프론트엔드-보안.md:54` — XSS 예제에서 `localStorage.token`을 처음 사용하지만 이 API가 어떤 저장소인지 설명하지 않습니다. “같은 오리진의 JS가 읽고 쓸 수 있고, 새로고침 뒤에도 남는 브라우저 저장소”라는 정의를 먼저 넣으세요. 토큰 표의 CSRF 비교도 원리 설명보다 먼저 나오므로, 131~154행의 CSRF 원리를 표 앞에 두거나 표 앞에서 자동 인증정보 전송 문제를 먼저 정의하세요.

- `docs/05-도구와-파이프라인/23-린트-테스트-CI-배포.md:72` — 테스트는 “상세가 열린다”고 이름 붙였지만 실제 검증은 콜백 호출뿐이고, 호출 기록을 남길 `fn`도 선언되어 있지 않습니다. `const fn = vi.fn()` 또는 `jest.fn()`을 선언하고 “선택한 주문 ID를 전달한다”로 테스트명을 바꾸거나, 부모 화면까지 렌더해 상세 표시를 검증하세요. `render`, `screen`, `userEvent`의 역할도 코드 앞에 한 줄씩 설명해야 읽기만 하는 독자가 검증 대상을 따라갈 수 있습니다.

## 3. 기술적 사실 오류

- `docs/05-도구와-파이프라인/21-패키지-매니저와-node_modules.md:29` — “Node는 그냥 둘 다 설치합니다”는 설치 주체와 실행 주체를 섞습니다. npm 같은 패키지 매니저가 버전별 파일을 배치하고, Node가 호출한 모듈의 위치를 기준으로 가까운 `node_modules`부터 찾는다고 구분하세요. 41행의 “충돌이 원천적으로 없습니다”도 삭제해야 합니다. 여러 버전의 공존이 가능해도, 앱과 렌더러가 서로 다른 React 사본을 쓰면 Hook 오류가 날 수 있습니다. [Node 모듈 해석](https://nodejs.org/api/modules.html#loading-from-node_modules-folders), [React 중복 설치 문제](https://react.dev/warnings/invalid-hook-call-warning#duplicate-react)

- `docs/05-도구와-파이프라인/21-패키지-매니저와-node_modules.md:47` — npm과 Yarn을 모두 평탄화 방식으로 묶고 81행 표에서도 반복합니다. 이는 Yarn Classic 또는 `node-modules` 설정에 한정해야 합니다. 현대 Yarn의 기본 설치 방식은 `node_modules` 없이 로더로 의존성을 해석하는 PnP입니다. 버전·설정에 따른 차이를 표에 반영하세요. [Yarn PnP](https://yarnpkg.com/features/pnp)

- `docs/05-도구와-파이프라인/21-패키지-매니저와-node_modules.md:84` — pnpm을 “프로젝트에는 전역 저장소로 향하는 링크만 있다”로 설명해 `.pnpm` 내부 구조와 두 종류의 링크가 사라졌습니다. 공유 저장소의 파일을 프로젝트 내부 `.pnpm`에 하드 링크 등으로 재사용하고, 심볼릭 링크로 패키지 간 의존 관계를 만든다는 구조를 보여주세요. 92행의 “유령 의존성이 아예 만들어지지 않는다”도 기본 격리 설정의 프로젝트 코드에 대한 설명으로 한정해야 합니다. 의존성 내부의 숨은 hoisting이나 공개 hoisting 설정까지 모두 차단되는 것은 아닙니다. [pnpm 링크 구조](https://pnpm.io/symlinked-node-modules-structure)

- `docs/05-도구와-파이프라인/21-패키지-매니저와-node_modules.md:101` — `npm ci` 설명에 실패 조건이 빠졌습니다. 기존 lockfile이 필요하고, `package.json`과 불일치하면 갱신하지 않고 실패한다는 두 조건을 명시하세요. CI 실패를 보고 `npm install`로 교체하는 오해를 막으려면, 로컬에서 의도한 의존성 변경과 lockfile을 함께 갱신·커밋하는 흐름까지 연결해야 합니다. [npm ci](https://docs.npmjs.com/cli/v11/commands/npm-ci/)

- `docs/05-도구와-파이프라인/21-패키지-매니저와-node_modules.md:152`, `docs/06-성능과-보안/27-프론트엔드-보안.md:178` — 빌드 도구 취약점을 “사용자에게 전송되지 않는” 경고로 낮추고, 오염된 서버 라이브러리는 방화벽 안에 있어 상대적으로 덜 위험하다고 대비합니다. 빌드·설치 중 실행되는 악성 코드는 CI 자격 증명과 배포 산출물을 공격할 수 있고, 서버 라이브러리는 서버 권한으로 데이터에 접근합니다. 전송 여부로 우선순위를 고정하지 말고 실행 위치·도달 가능성·권한·피해 범위로 판단하도록 바꾸세요. 27장 188행의 `postinstall` 경고와도 일치시켜야 합니다.

- `docs/05-도구와-파이프라인/22-번들러와-빌드.md:25` — `data.map(o => <Row ... />)`는 주문 데이터가 아니라 React element 배열을 만드는데 결과를 `Order[]`로 선언했습니다. 타입 표기를 제거해 추론에 맡기거나, 데이터 배열과 JSX 배열을 별도 변수로 구분하세요. TS·JSX 변환을 설명하는 예시에 의도하지 않은 타입 오류가 섞여 있습니다.

- `docs/05-도구와-파이프라인/22-번들러와-빌드.md:65`, `docs/05-도구와-파이프라인/23-린트-테스트-CI-배포.md:120` — “별도의 `tsc --noEmit`이 없으면 타입 에러가 그대로 배포된다”는 결론은 예시에 있는 `next build`와 충돌합니다. Next.js는 기본적으로 빌드 중 타입 검사를 하고 오류가 있으면 실패합니다. Babel/SWC/esbuild의 변환 단계와 프레임워크의 전체 빌드 파이프라인을 구분하고, 별도 타입 검사는 빠른 실패를 위한 단계로 설명하세요. 같은 단정이 있는 22장 148·162행, 23장 29·128·196·209행과 마지막 질문도 함께 수정해야 합니다. [Next.js TypeScript 검사](https://nextjs.org/docs/app/api-reference/config/typescript#disabling-typescript-errors-in-production)

- `docs/05-도구와-파이프라인/22-번들러와-빌드.md:81` — “ESM이어야 하고 부수 효과가 없어야 한다”는 조건을 패키지 전체에 적용하면, 부수 효과가 있는 패키지는 아무 코드도 제거할 수 없는 것으로 읽힙니다. ESM의 정적 구조가 분석에 유리하다는 점, 미사용 export 제거와 모듈 전체 제거의 차이, 필요한 부수 효과는 보존한다는 점을 구분하세요. 154행도 “부수 효과가 없어야 합니다” 대신 “제거 대상에 필요한 부수 효과가 없어야 합니다”로 좁히세요. [webpack tree shaking](https://webpack.js.org/guides/tree-shaking/)

- `docs/05-도구와-파이프라인/22-번들러와-빌드.md:111` — HMR이면 항상 해당 모듈만 바뀌고 입력 상태가 유지된다고 설명합니다. React의 상태 보존은 Fast Refresh가 가능한 변경에 한정되며, export 구조나 컴포넌트 변경에 따라 재마운트·전체 새로고침이 일어납니다. HMR과 React 상태 보존을 구분하고 예외를 한 줄 추가하세요. [Next.js Fast Refresh](https://nextjs.org/docs/architecture/fast-refresh)

- `docs/05-도구와-파이프라인/22-번들러와-빌드.md:120` — Vite가 개발 중 “번들하지 않는다”는 설명은 의존성 사전 번들링을 빠뜨렸습니다. 앱 소스는 필요한 모듈을 요청받아 제공하지만 의존성은 사전 번들링할 수 있다고 범위를 나누세요. [Vite dependency pre-bundling](https://vite.dev/guide/dep-pre-bundling.html)

- `docs/05-도구와-파이프라인/22-번들러와-빌드.md:131`, `docs/06-성능과-보안/26-네트워크와-에셋-최적화.md:106` — `next build` 출력에서 라우트별 번들 크기·첫 화면 JS 크기를 확인하라는 안내는 Next.js 16 기준으로 낡았습니다. 16부터 `size`와 `First Load JS` 출력이 제거됐습니다. 프로덕션 빌드를 띄운 뒤 Network 탭에서 초기 JS 전송량을 확인하는 방법이나 해당 번들러용 분석 도구로 바꾸고, 가이드의 Next.js 기준 버전을 명시하세요. [Next.js 16 변경 사항](https://nextjs.org/docs/app/guides/upgrading/version-16#performance-improvements)

- `docs/05-도구와-파이프라인/23-린트-테스트-CI-배포.md:132` — 직전 파이프라인은 `next build`인데, 배포 설명은 결과물을 전부 정적 파일로 보고 실행 프로세스가 없다고 합니다. 142행의 뒤늦은 예외만으로는 독자가 어떤 결과물을 어디에 배포하는지 알 수 없습니다. 절의 첫 문장에서 브라우저 전용 앱의 정적 빌드에 한정하고, Next.js 일반 빌드는 서버 실행이 포함될 수 있으며 정적 배포에는 별도 조건이 있다고 연결하세요. [Next.js 배포 방식](https://nextjs.org/docs/app/getting-started/deploying)

- `docs/06-성능과-보안/24-Core-Web-Vitals.md:19` — 페이지 체감 시간을 순차 단계의 합으로 그리고 “Core Web Vitals는 이 전체를 세 조각으로 나눠 잰다”고 설명합니다. 다운로드·파싱·실행·그리기는 겹칠 수 있고, LCP·INP·CLS는 전체 시간을 분할하는 세 구간이 아닙니다. 그림을 의존 관계의 개략도로 한정하고, 세 지표는 로딩·상호작용 지연·시각적 안정성이라는 서로 다른 관점을 측정한다고 바꾸세요.

- `docs/06-성능과-보안/24-Core-Web-Vitals.md:41` — LCP를 모든 종류의 “가장 큰 요소”로 정의하면 큰 빈 컨테이너도 대상이라고 오해합니다. “페이지 탐색 시작부터 초기 화면에 보이는 이미지·텍스트 블록 등 LCP 후보 중 가장 큰 콘텐츠가 그려질 때까지”로 범위와 시간 기준을 명시하세요. 43행의 “이 페이지가 무엇인지 알 수 있는 시점”은 목적을 설명하는 비유이며, 실제 콘텐츠 완성도를 판단하는 지표는 아니라고 구분해야 합니다. [LCP 정의](https://web.dev/articles/lcp)

- `docs/06-성능과-보안/24-Core-Web-Vitals.md:58` — INP의 “세션 전체에서 나쁜 쪽”만으로는 무엇을 모아 대표값을 만드는지 알 수 없습니다. 한 페이지 방문 중 클릭·탭·키보드 상호작용에서 입력 대기, 핸들러 처리, 다음 페인트까지를 재고, 보통 가장 느린 값을 사용하되 상호작용 50개마다 최악의 값 하나를 제외한다고 설명하세요. API 응답이 완료될 때까지의 전체 시간이나 스크롤·호버의 지연을 뜻하지 않습니다. 63행도 실행 중 어느 시점에 클릭했는지에 따라 대기 시간이 달라지므로 “400ms 뒤”를 “남은 실행 시간만큼 기다릴 수 있다”로 고치세요. [INP 측정 방식](https://web.dev/articles/inp)

- `docs/06-성능과-보안/24-Core-Web-Vitals.md:79` — CLS를 단순 “누적 점수”로만 설명하면 페이지가 오래 열릴수록 모든 이동을 계속 합산하는 옛 정의로 읽힙니다. 현재는 예상하지 못한 이동을 묶은 구간 중 합계가 가장 큰 구간을 사용합니다. 구간은 이동 사이 간격 1초 미만, 최대 길이 5초이고, 사용자 입력 직후 일정 시간 내의 예상된 이동은 제외된다고 설명하세요. 오래 열어두는 관리자 화면을 대상으로 하므로 이 구분이 필요합니다. [CLS 정의](https://web.dev/articles/cls)

- `docs/06-성능과-보안/24-Core-Web-Vitals.md:103` — Lab의 한계를 “내 맥북 + 사내 기가망 기준”으로 규정하지만 Lighthouse는 CPU·네트워크 조건을 제한·시뮬레이션할 수 있습니다. 실제 한계는 선택한 기기·망·시나리오가 실사용자 분포를 대표하지 못한다는 점입니다. 115행의 기본 탐색 분석만으로 INP도 측정되는 듯한 안내도 고치세요. INP 진단에는 실제 상호작용을 수행하는 Performance 패널 등의 절차가 필요합니다. [Lab·field 차이](https://web.dev/articles/lab-and-field-data-differences), [DevTools Performance](https://developer.chrome.com/docs/devtools/performance/overview)

- `docs/06-성능과-보안/24-Core-Web-Vitals.md:135` — `sendBeacon`은 페이지 이탈을 방해하지 않으면서 전송을 시도하는 API이지 서버 수신을 보장하는 API가 아닙니다. 반환값 `true`도 전송 큐에 들어갔다는 뜻입니다. “전송이 보장된다”를 고치고, 지표 수집에 누락이 있을 수 있음을 명시하세요. [sendBeacon 반환값](https://developer.mozilla.org/en-US/docs/Web/API/Navigator/sendBeacon#return_value)

- `docs/06-성능과-보안/24-Core-Web-Vitals.md:152` — 75퍼센타일을 “사용자 4명 중 3명”으로 풀이했지만 평가 단위는 고유 사용자 수가 아닌 페이지 방문 표본입니다. “기기군별 페이지 방문의 최소 75%가 기준 이내”로 고치고, LCP·INP·CLS 각각의 p75가 좋음 기준을 만족해야 한다고 연결하세요. 한 페이지 안의 INP 대표값 계산과 여러 방문을 집계한 p75도 구분해야 합니다. [INP 집계](https://web.dev/articles/inp), [CLS 평가 기준](https://web.dev/articles/cls)

- `docs/06-성능과-보안/25-렌더링-성능.md:22` — 렌더·재조정은 싸고 커밋은 비싸다고 고정하고, 화면이 같으면 커밋 자체가 0번이라고 결론냅니다. 렌더 계산이 병목일 수 있으며 DOM 변경이 없더라도 커밋에 따른 Effect 실행은 가능합니다. “비용은 작업량에 따라 달라진다”, “계산 결과가 같으면 DOM 변경이 필요 없을 수 있다”로 나누세요. 뒤에서 무거운 렌더 계산을 주요 원인으로 설명하는 내용과도 충돌합니다. [React useEffect](https://react.dev/reference/react/useEffect)

- `docs/06-성능과-보안/25-렌더링-성능.md:51` — Profiler 가로 막대 길이를 해당 컴포넌트 자신의 이번 렌더 시간으로 읽게 합니다. Flame chart의 폭은 자식까지 포함한 마지막 렌더 시간이고, 현재 선택한 commit의 작업은 색상과 상세 수치를 함께 봐야 합니다. 넓은 부모 막대가 자식들의 합일 수 있으므로 64·225·249행의 “긴 막대 하나를 찾는다”도 하위 트리와 실제 비용을 확인하는 절차로 바꾸세요. [React Profiler 차트 설명](https://legacy.reactjs.org/blog/2018/09/10/introducing-the-react-profiler.html#flame-chart)

- `docs/06-성능과-보안/25-렌더링-성능.md:135` — props가 같으면 `memo`가 함수 실행을 건너뛴다고만 하면 자체 state나 구독 Context 변경까지 막는 것으로 읽힙니다. 부모의 재렌더에 따른 호출을 줄이는 최적화이며 자체 state·Context 변경은 계속 렌더를 일으킨다고 명시하세요. 170·251행의 “혼자서는 무용지물”도 틀립니다. 원시값 props나 이미 안정적인 참조를 받으면 `useMemo`·`useCallback` 없이도 유효합니다. [React memo](https://react.dev/reference/react/memo)

- `docs/06-성능과-보안/25-렌더링-성능.md:164` — `handleSelect`와 `rowStyle` 선언만 추가해도 앞 예시의 `onSelect={() => select(o.id)}`와 `style={{ padding: 8 }}`는 계속 새 값입니다. 실제로 `<Row onSelect={handleSelect} style={rowStyle} ... />`를 전달하고 Row 안에서 `onSelect(order.id)`를 호출하는 수정까지 보여주세요. Hook을 Table 최상위에서 호출한다는 위치와 `select`가 모듈 함수인지 props인지도 명시해야 `[]`의 타당성을 판단할 수 있습니다.

- `docs/06-성능과-보안/25-렌더링-성능.md:90` — `useMemo`의 의존성 비교 기준과 캐시의 한계를 빠뜨렸습니다. “각 의존성을 `Object.is`로 비교한다”와 “성능 최적화용 캐시여서 값의 영구 보존을 보장하지 않는다”를 추가하세요. `useCallback`도 함수 생성을 없애는 API가 아니라 이전 함수 참조를 반환할 수 있게 하는 API입니다. 182행의 “함수를 만드는 비용은 사실상 0” 대신, 최적화 대상이 생성 비용인지 참조 변화에 따른 후속 작업인지 구분하세요. [useMemo](https://react.dev/reference/react/useMemo), [useCallback](https://react.dev/reference/react/useCallback)

- `docs/06-성능과-보안/25-렌더링-성능.md:211` — index key 수정이 “어떤 메모이제이션보다 효과가 크다”는 우선순위는 보장할 수 없습니다. index key의 핵심 위험은 삽입·삭제·정렬 시 항목과 state의 대응이 어긋나는 것이며, 단순 정렬에서는 안정적 key 사용이 오히려 DOM 이동을 추가할 수도 있습니다. 올바른 ID를 사용하는 이유는 동일성 보존으로 설명하고, 성능 개선 폭은 측정 결과로 판단하세요. [React 목록의 key](https://react.dev/learn/rendering-lists#keeping-list-items-in-order-with-key)

- `docs/06-성능과-보안/25-렌더링-성능.md:215` — React Compiler를 “React 19와 함께 나온 컴파일러”로 묶으면 React 버전 자체의 기능·배포 주기와 혼동됩니다. 별도로 빌드에 적용하는 도구이고 React 17 이상도 지원하며, 1.0 안정판은 2025년 10월 출시됐다고 고치세요. 앞의 수동 memo 예시는 Compiler를 적용하지 않은 상태를 전제로 한다는 점도 명시하세요. [React Compiler 1.0](https://react.dev/blog/2025/10/07/react-compiler-1)

- `docs/06-성능과-보안/26-네트워크와-에셋-최적화.md:47` — AVIF를 항상 “가장 작음”, PNG를 “투명이 필요할 때만”으로 분류할 수 없습니다. 압축 결과는 이미지 내용·화질·인코딩 설정에 따라 달라지고 WebP·AVIF도 투명도를 지원합니다. 사진의 손실 압축, 글자·도형의 무손실 표현, 투명도와 호환성을 기준으로 설명하고 JPEG 800KB → WebP 300KB 수치는 특정 이미지의 예시로 표시하세요. [이미지 포맷 비교](https://developer.mozilla.org/en-US/docs/Web/Media/Guides/Formats/Image_types)

- `docs/06-성능과-보안/26-네트워크와-에셋-최적화.md:95` — `swap`의 “글자는 항상 보임”은 매우 짧은 차단 구간을 생략한 단정입니다. `optional`의 “CLS가 없음”도 늦은 폰트 교체로 인한 이동을 피한다는 범위로 좁혀야 합니다. 98행의 “굵기 하나가 곧 파일 하나”는 정적 폰트에만 해당하며, 가변 폰트는 한 파일로 여러 굵기를 제공할 수 있다고 덧붙이세요. [font-display](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/At-rules/@font-face/font-display), [가변 폰트](https://developer.mozilla.org/en-US/docs/Web/CSS/Guides/Fonts/Variable_fonts)

- `docs/06-성능과-보안/26-네트워크와-에셋-최적화.md:120` — `max-age`와 `ETag`를 서로 다른 캐싱 방식으로 대립시킵니다. `max-age`는 신선도 수명이고 `ETag`는 재검증 때 비교하는 식별자라 함께 쓸 수 있습니다. `ETag` 자체가 매번 요청을 강제하지 않으며, 캐시가 stale해졌거나 `no-cache` 등으로 재검증이 필요한 경우 `If-None-Match`를 보내고, 일치할 때 304를 받는다고 고치세요. [HTTP 캐시 재검증](https://developer.mozilla.org/en-US/docs/Web/HTTP/Guides/Caching#validation)

- `docs/06-성능과-보안/26-네트워크와-에셋-최적화.md:144`, `docs/05-도구와-파이프라인/23-린트-테스트-CI-배포.md:184` — HTML을 “캐시하지 않는다”고 설명하면서 실제로는 `no-cache`를 사용해 바로 다음 설명과 모순됩니다. “저장할 수 있지만 재사용 전에 재검증한다”로 통일하세요. 26장 137·202행의 “영원히 캐시”도 해시 주소의 내용이 불변이라는 뜻과 실제 캐시 수명을 구분해야 합니다. 예시의 유효 수명은 `max-age`에 지정한 1년입니다. `immutable`은 신선한 기간에 내용이 바뀌지 않음을 알리는 지시자이며, 무기한 보관이나 캐시 삭제 방지를 보장하지 않습니다. [HTTP 캐싱](https://developer.mozilla.org/en-US/docs/Web/HTTP/Guides/Caching)

- `docs/06-성능과-보안/26-네트워크와-에셋-최적화.md:156` — CDN이 줄인 왕복 시간을 요청 수만큼 곱하는 설명은 모든 요청이 직렬이라는 전제가 필요합니다. 요청은 병렬로 진행되거나 한 연결에서 다중화될 수 있습니다. “서로 의존하는 요청이 여러 단계 이어지면 왕복 지연이 누적된다”로 고치고, 요청 개수와 직렬 단계 수를 구분하세요.

- `docs/06-성능과-보안/27-프론트엔드-보안.md:91` — React 19에서 `<a href={user.website}>`에 `javascript:`를 주면 공격 코드가 실행된다는 예시는 틀렸습니다. React 19는 `href`·`src`의 해당 URL을 차단합니다. URL 검증은 여전히 필요하지만 이 예시의 근거는 바꾸세요. 허용된 HTTPS 주소도 피싱 사이트일 수 있고, 직접 DOM을 조작하는 라이브러리는 React의 차단을 거치지 않는다는 식으로 방어 범위를 구분할 수 있습니다. [React 19 변경 사항](https://react.dev/blog/2024/04/25/react-19-upgrade-guide#other-breaking-changes)

- `docs/06-성능과-보안/27-프론트엔드-보안.md:113` — 표의 열 제목 “XSS에”와 HttpOnly 행의 ✅는 XSS 자체를 방어하는 것으로 읽힙니다. 열을 “XSS 시 토큰 직접 읽기”로 좁히고, HttpOnly여도 공격 스크립트가 같은 오리진 API를 호출하고 응답을 읽을 수 있음을 바로 아래에 넣으세요. `localStorage`의 CSRF “안전”도 저장 위치 때문이 아니라 토큰을 JS가 명시적으로 헤더에 넣고 서버가 그 인증 방식만 받는다는 전제에 달려 있습니다. [HttpOnly](https://developer.mozilla.org/en-US/docs/Web/HTTP/Reference/Headers/Set-Cookie#httponly), [OWASP CSRF 방어](https://cheatsheetseries.owasp.org/cheatsheets/Cross-Site_Request_Forgery_Prevention_Cheat_Sheet.html)

- `docs/06-성능과-보안/27-프론트엔드-보안.md:127` — `SameSite=Lax`는 다른 사이트에서 시작된 모든 요청에서 쿠키를 빼는 설정이 아닙니다. 교차 사이트라도 최상위 화면 이동의 GET 같은 안전한 메서드에는 포함될 수 있습니다. 150행도 수정하고, site는 3장의 origin과 달리 스킴과 등록 가능한 도메인을 기준으로 구분하므로 서로 다른 서브도메인도 same-site일 수 있다는 정의를 추가하세요. 137~144행의 교차 사이트 POST 공격은 직전에 제시한 명시적 Lax 쿠키로는 그대로 재현되지 않으므로, 방어가 적용되지 않은 쿠키를 전제로 표시해야 합니다. [SameSite 동작](https://developer.mozilla.org/en-US/docs/Web/HTTP/Reference/Headers/Set-Cookie#samesitesamesite-value)

- `docs/06-성능과-보안/27-프론트엔드-보안.md:154` — SameSite만 붙이면 쿠키 인증의 CSRF 대응이 끝나고 헤더 인증은 “구조적으로 발생하지 않는다”는 단정은 과합니다. 상태 변경에 안전한 메서드를 쓰지 않는 규칙과 서버의 CSRF 토큰 또는 Origin 검증을 연결하세요. 헤더 방식은 브라우저가 인증정보를 자동 첨부하는 전통적 공격을 줄이지만, 공격자가 제어하는 URL 등을 신뢰해 앱 JS가 인증 헤더를 붙이는 client-side CSRF까지 배제하지는 않습니다. 입문에서는 “명시적 헤더 인증은 일반적인 교차 사이트 폼 공격에 강하다”로 범위를 좁히면 됩니다. [OWASP CSRF 한계와 방어](https://cheatsheetseries.owasp.org/cheatsheets/Cross-Site_Request_Forgery_Prevention_Cheat_Sheet.html)

- `docs/06-성능과-보안/27-프론트엔드-보안.md:164` — CSP 예시가 `img-src ... https:`로 모든 HTTPS 이미지 주소를 허용하는데, 168~170행은 악성 스크립트의 외부 데이터 전송을 막는다고 단정합니다. 이 정책에서도 이미지 URL의 쿼리에 데이터를 넣으면 HTTPS 외부 주소로 요청할 수 있습니다. `connect-src`가 막는 요청과 `img-src`가 허용하는 요청을 구분하고 필요한 출처만 허용하도록 예시를 좁히세요. CSP를 모든 외부 전송을 통제하는 방화벽과 동일시하는 비유도 완화해야 합니다. [CSP img-src](https://developer.mozilla.org/en-US/docs/Web/HTTP/Reference/Headers/Content-Security-Policy/img-src)

## 4. 한글 가독성

- `docs/05-도구와-파이프라인/21-패키지-매니저와-node_modules.md:9` — “라이브러리가 또 다른 라이브러리를 끌고 오고, 그게 또 끌고 옵니다”는 `그게`가 가리키는 대상과 연쇄 관계를 독자가 다시 추적하게 합니다. “직접 선언한 라이브러리도 다른 라이브러리에 의존합니다. 이 의존 관계는 여러 단계로 이어집니다”로 바꾸세요.

- `docs/05-도구와-파이프라인/22-번들러와-빌드.md:88` — “화면이 30개인데 … 수도 있는데 말이죠”처럼 `-는데`를 거듭 연결합니다. “대시보드에는 화면이 30개 있어도 사용자는 목록 하나만 보고 나갈 수 있습니다. 모든 화면의 코드를 처음부터 받을 필요는 없습니다”로 두 문장으로 나누세요.

- `docs/06-성능과-보안/24-Core-Web-Vitals.md:60` — “6장이 그대로 회수되는 지표”는 독자에게 개념을 설명하기보다 집필상의 장치로 표현한 문장입니다. “6장에서 배웠듯 메인 스레드가 바쁘면 입력 처리가 늦어집니다. 이 지연이 INP에 드러납니다”로 바꾸세요. 같은 장 92행의 “마지막 항목이 실무에서 자주 놓칩니다”는 주어도 어긋나므로 “데이터가 도착한 뒤 생기는 높이 변화를 실무에서 자주 놓칩니다”로 고치세요.

- `docs/06-성능과-보안/25-렌더링-성능.md:37` — “그래서 무엇인지부터 알아야 합니다”에서 `무엇`이 해법인지 원인인지 불분명합니다. “세 원인 중 어느 것이 지연을 만드는지 먼저 측정해야 합니다”로 바꾸세요.

- `docs/06-성능과-보안/26-네트워크와-에셋-최적화.md:180` — 진단 순서의 “폰트 굵기 개수를 줄이고 font-display”는 서술어가 없어 행동이 완결되지 않습니다. “필요한 굵기만 로드하고, 늦은 폰트 교체를 허용할지에 따라 `font-display`를 고른다”처럼 점검 내용을 끝까지 쓰세요.

- `docs/06-성능과-보안/27-프론트엔드-보안.md:192` — “우리 페이지를 투명한 iframe으로 덮어”는 공격자 페이지와 피해 페이지 중 어느 쪽을 iframe에 넣는지 모호합니다. “공격자가 자신의 페이지 위에 우리 사이트를 담은 투명한 iframe을 겹칩니다. 사용자는 미끼 버튼을 누른다고 생각하지만 iframe 속 버튼을 누르게 됩니다”로 주체와 배치를 명확히 하세요.

## 5. 톤·장 구조

- `docs/05-도구와-파이프라인/23-린트-테스트-CI-배포.md:15`, `docs/06-성능과-보안/25-렌더링-성능.md:17` — BE 연결 직후에 각각 도구 분류와 렌더 비용 설명으로 들어가고, 이를 필요로 하는 구체적 문제는 뒤에 나옵니다. 요청한 `문제 상황 → 개념` 순서를 맞추려면 23장은 “빌드는 됐는데 선택 동작이 깨진 PR”, 25장은 “검색창에 한 글자 입력할 때 표가 멈추는 상황”을 먼저 제시하고 뒤의 검사·측정 설명이 그 문제를 풀도록 연결하세요.

- `docs/05-도구와-파이프라인/23-린트-테스트-CI-배포.md:3` — 장의 질문은 테스트 대상만 묻는데, 핵심 3가지와 본문의 상당 부분은 CI·배포·구버전 공존을 다룹니다. “프론트엔드 변경을 무엇으로 검증하고, 배포 뒤에는 무엇을 지켜야 하는가?”처럼 실제 범위와 질문을 맞추고, `스스로 답해보기` 두 번째 문항에도 배포 후 호환성 판단을 반영하세요.

- `docs/06-성능과-보안/25-렌더링-성능.md:92`, `docs/06-성능과-보안/25-렌더링-성능.md:176`, `docs/06-성능과-보안/25-렌더링-성능.md:224` — 기존 20장 89~91행의 범위를 한정하는 설명과 달리, “배열 100개를 필터하는 건 0.01ms”, “세 경우뿐”, “예외 없음”이 새 장의 조언을 절대 규칙으로 만듭니다. 환경·콜백에 따라 달라지는 숫자는 예시라고 표시하고, 사용처는 대표 사례로, Profiler 절차는 React 렌더 병목이 의심될 때의 시작점으로 한정하세요. 24장 207행의 “INP는 대체로 React 코드가 만듭니다”도 같은 장 74~75행의 JSON 파싱·서드파티 원인을 가리므로 “다음 장에서는 INP에 영향을 주는 원인 중 React 렌더 작업을 살펴봅니다”로 연결하세요.
